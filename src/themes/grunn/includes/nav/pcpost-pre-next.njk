{# POST PRE NEXT NAVIGATION
------------------------------------------------
A simple pre / next nav for collections

 #}
{%- if env.mode != 'prod' %}
  <!-- __includes/nav/post-pre-next.njk  -->
{%- endif  -%}
PRE
{%- if collections.thepc %}

  <nav class="flex flex-row justify-between w-full p-4" aria-label="Pre and Next post">
    {#  #}
    {%- set nextPost = collections.thepc | getNextCollectionItem(page) %}
    {%- set previousPost = collections.thepc | getPreviousCollectionItem(page) %}

    {%- if nextPost or previousPost %}
      {# previous  link #}
      {%- if previousPost %}
        <a href="{{ previousPost.url | url }}" title="{{ previousPost.data.title }}" class="flex flex-row items-center w-full">
          <div class="w-4  mr-2  text-black dark:text-white ">{% include "icons/arrow-left.svg" %}</div>
          <div>{{ previousPost.data.title }}</div>
        </a>
      {% endif %}
      {# Next  linl #}
      {%- if nextPost %}
        <a href="{{ nextPost.url | url }}" title="{{ nextPost.data.title }}" class="flex flex-row items-center justify-end w-full">
          <div>{{ nextPost.data.title }}</div>
          <div class="w-4 ml-2 text-black dark:text-white ">{% include "icons/arrow-right.svg" %}</div>
        </a>
      {% endif %}
      {# /if pre of next #}
    {%- endif %}
  </nav>

{%- endif %}