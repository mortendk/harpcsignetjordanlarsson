---
layout: default.njk
saga-template-name: Page default
---
{# PAGE DEFAULT TEAMPLATE
------------------------------------------------
This is the default template for the page type
#}
{%- if env.mode != 'prod' %}
  <!-- ___layouts/page/page.njk  -->
{%- endif -%}

<h1 class="text-center text-5xl lg:text-8xl pt-12 pb-4 pagewidth drop-shadow-sm">{{ title }}</h1>

{% if image %}
  <div class="aspect-video overflow-hidden">
    {% picture
      img=image,
      width=[640,1024, 1563],
      sizes="(max-width: 640px) 50vw, 100vw",
      css="shadow rounded-xl",
      loading = "eager"
  %}
  </div>
{% endif %}

{% if content %}
  <div class="prose-lg  max max-w-prose  pagewidth">
    {{ content | markdown | safe }}
  </div>
{% endif %}

<div class="pagewidth">
  {% include "components/all.njk" %}
</div>

{% include "share.njk" %}